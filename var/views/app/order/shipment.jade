include ../mixins/control
include ../mixins/progress

section#order-shipment
  div.row
    div.col-md-6.col-md-offset-3
      +progress(50)
  div.row
    div.col-md-4.col-md-offset-2
      form(role='form' autocomplete='on')
        fieldset
          div.row
            div.col-md-6
              +control('name-first', 'Vorname')(
                type='text', value=order && order.shipment.name.first,
                placeholder='Max', required='required')
            div.col-md-6
              +control('name-last', 'Nachname')(
                type='text', value=order && order.shipment.name.last,
                placeholder='Mustermann', required='required')
          div.row
            div.col-md-8
              +control('address-street', 'Straße')(
                type='text', value=order && order.shipment.address.street,
                placeholder='Hauptstr.', required='required')
            div.col-md-4
              +control('address-streetNr', 'Nr.')(
                type='text', value=order && order.shipment.address.streetNr,
                placeholder='4A', required='required')
          div.row
            div.col-md-6
              +control('address-zipCode', 'Postleitzahl')(
                type='number', value=order && order.shipment.address.zipCode,
                placeholder='12345', required='required',
                min='10000' max='99999')
            div.col-md-6
              +control('address-place', 'Ort')(
                type='text', value=order && order.shipment.address.place,
                placeholder='Berlin', required='required')
          button.btn.btn-sm.btn-primary Fortfahren
    div.col-md-4
      h4.text-center Versandinformationen
      p.text-justify
        | Aktuell sind nur Lieferungen innerhalb Deutschlands möglich.
        | Standard Versand ist per Deutsche Post Maxibrief. Falls dies
        | versandtechnisch auf Grund der Größe Ihrer Bestellung nicht möglich
        | ist, bekommen Sie die Ware per DHL Päckchen beziehungsweise der
        | nächstmöglichen Größe zugeschickt.
